# Copy to .env.local and fill in. All values are optional.

# Canonical site URL (used by metadata, sitemap, robots).
NEXT_PUBLIC_SITE_URL=https://your-deployment.vercel.app

# Optional contact-form email delivery (via Resend). Leave unset for demo mode.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
